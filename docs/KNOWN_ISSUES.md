# 已知问题与解决方案

> 对应 AGENTS.md §12（异常处理）与 §8.6（裁剪率 <0.85 先报告数据）。
> 状态图例：**已解决** / **已缓解** / **非缺陷（需如实说明）** / **待定（需用户决策）**

## 已解决

| # | 问题 | 根因 | 解决方案 |
|---|---|---|---|
| 1 | 自研 Harris 角点定位误差约 1.4 px，不满足 §11 的 ≤1 px | 结构张量盒式滤波窗口 k=5 时响应峰偏向角点内侧 | 改 `BOX_SIZE=3`（实测峰值正落角点像素，误差 0.000 px），理由写入源码常量注释 |
| 2 | 中值平滑器偶发 `IndexError: peek from empty` 且数值与朴素实现不符 | 双堆延迟删除顺序错误：先清理堆顶再分类，会把待删堆顶移走导致分类错判与空堆 peek | 改为「先按干净堆顶分类 → 再清理」，`_value()` 增加防御性清理 |
| 3 | 镜头切分单测中「平滑运动」MAD 反而大于阈值 | 测试用例用随机噪声图构造连续帧，噪声本身 MAD 极大 | 改用平滑渐变图做连续帧、随机图做切换帧 |
| 4 | 自研 warp 与 cv2 对照时整幅错位（PSNR ≈ 9 dB） | OpenCV 5.0.0 的 `warpAffine` 改为「输入→输出」正向约定，与 ≤4 相反 | §7 注释更正；测试直接传 `M[:2]`（不再取逆） |
| 5 | `cv2.warpAffine` 抛 `M0.rows == 2 && M0.cols == 3` | 传入 3×3 齐次矩阵 | 对照调用改为 `M[:2]` |
| 6 | 合成轨迹幅度约束（帧间 ≤5 px、累积 ≤40 px）不可控，100 次重试全失败 | OU 过程单次实现的样本 std 波动极大（长记忆下有效样本仅个位数），理论 std 失效 | 改为「白噪声经高斯核平滑的增量过程 + 构造性限幅」（增量与累积分别硬限幅），首次即通过 |
| 7 | pass 2 单帧 345 ms，test1 预计 20 min | 网格逐帧重建 + float64 + warp 与 crop 两次重采样 | 输出网格缓存 + uint8 走 float32 + warp/裁剪/缩放**复合单次重采样**；提速约 1.8×（test1 pass2 ≈ 11 min） |
| 8 | 本机 CMD 中 `python -m venv` 生成的 venv 无法激活（无 activate.bat） | `python` 解析到 msys2 的 Python 3.12.11，其 venv 为 `bin/` 布局 | 用 CPython 3.13 完整路径重建标准 `Scripts/` 布局；README 增加本机备注 |
| 9 | pip 安装依赖失败（清华镜像返回空索引、官方源挂起） | 本机网络对部分 PyPI 站点不可用 | 改用阿里云镜像；README 提供镜像/代理/离线三种方案 |
| 10 | test1.mp4 裁剪率 0.786 < 0.85 | 素材为多镜头剪辑，按单镜头假设累积全局轨迹导致切换污染 | 方案①镜头切分（src/shots.py）：切换处轨迹重置、逐镜头平滑/限幅、S 逐镜头聚合 → 0.8792（M0）/ 0.981（M1） |
| 11 | 切换检测假阴性：MAD 实测切换点 508/809/880 未检出（仅 1263 命中） | **跟踪点集随时间退化**：存活 ≥30 即不重检测，存活者偏向跨场景静态结构（齿孔/片框/暗部平坦区），到切换帧时存活率仅 ~0.55（不达 0.25 线）、内点率 0.8+，两路证据同时失效；此前文档记载的「存活 30/304 = 0.099」系新鲜全集诊断数字，退化点集不可达（2026-09-25 复现诊断实锤：508/809/880 管线实测存活率 0.547/0.550/0.581） | **无状态探针**（2026-09-25，用户批准）：MAD>25 候选帧上 `probe_cut_evidence` 现检角点 + 单步跟踪独立取证（新鲜全集），崩溃线 0.45（切换帧 0.099–0.237 vs 常态 0.656–1.000 取中点），`is_cut` 两路证据取或。**最终回归：4/4 全检出 [508, 809, 880, 1263]**，ITF 增益 +0.650→+0.687，裁剪率 0.981→0.972（均达标），pytest 新增 5 项 |
| 20 | 覆盖率工具缺失，§11 的 ≥80% 验收项无法量化 | pytest-cov / coverage 属新增依赖，§3 要求先经用户确认 | 用户批准（2026-09-25）：pytest-cov 7.1.0 + coverage 7.16.1 锁定入 requirements.txt；实测核心模块覆盖率 **98%**（最低 95%），达标 |

## 已缓解

| # | 问题 | 现状与残余 |
|---|---|---|
| （当前无） | — | 探针落地后原 #11 已转「已解决」 |

## 非缺陷（报告需如实说明）

| # | 现象 | 说明 |
|---|---|---|
| 12 | 实测中 RingBuffer 慢于 `list.pop(0)`、双堆中值在小 k 慢于 `sorted` | Python 常数因子掩盖渐进优势（C 实现的 memmove / Timsort 很快）。k 增大后差距按预期收敛/反超（环形缓冲 0.49→0.64×，双堆 k=127 达 1.03×）。报告须两层结论都写 |
| 13 | 居中平滑存在 r = window//2 帧延迟 | 离线口径，不适用于实时场景（§2 局限性要求） |
| 14 | ITF 语义局限 | 「静相机 + 动目标」场景下帧间差 ≠ 不稳定，ITF 会被误判（§2） |
| 15 | 4 自由度模型不含透视/视差 | 相似变换的固有局限（§2） |
| 16 | 测试素材与合同预期不符 | test1.mp4 实为老电影胶片扫描件（齿孔/圆角片框/「测试样片」水印）且含多镜头剪辑，与「手持抖动视频」分布不同；其抖动来源是片门晃动 |
| 17 | pass 2 耗时较长 | test1.mp4（1440 帧）约 11 min；可选优化：合并诊断 warp 与写出、按需关闭掩膜 ITF 诊断 |
| 18 | 产物不入库 | `output/`、`data/*.mp4` 均 gitignore（体积大、可复现）；跨机迁移需手动携带 test1.mp4 |
| 19 | M1 实拍 ITF 提升弱于 M0（+0.65 vs +1.30 dB） | **残差阈值实验证伪**（2026-09-25，用户批准）：0.05→0.075 使 ITF 增益 +0.650→+0.614（放宽只引入暗部平坦区低质量存活点），已回退 0.05；差距归因于 M0 的金字塔 LK（`calcOpticalFlowPyrLK`），§8.2 将金字塔列为可选升级项。**用户决策**：差距文档化，不追加实验轮（§11 验收线仅要求 ITF 高于原视频，+0.65 dB 达标；金字塔升级留作课程加分项） |

## 待定（需用户决策）

| # | 问题 | 数据 | 候选方案与代价 |
|---|---|---|---|
| （当前无） | 三项遗留问题（#11 切换假阴性 / #19 ITF 差距 / #20 覆盖率工具）已于 2026-09-25 全部收口，处置见「已解决 #11、#20」与「非缺陷 #19」 | — | — |

## 处理原则

- 任何指标不达标：**先报告数据、与用户讨论**，禁止擅自放大裁剪、降低标准或改验收线（§8.6 / §11）。
- 阈值调整（LK 残差、切换判据等）属于实现参数，§8.2 允许 ±50% 内调整，但**必须记入 PROJECT_STATE.md 决策日志**。
